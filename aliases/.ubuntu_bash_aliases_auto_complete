# Getting set up
# Create a ~/.bash_completion file with the following contents:
~/.bash_completion.d/complete_alias

# Add this after the instatiation of the bash_aliases in ~/.bashrc
complete -F _complete_alias "${!BASH_ALIASES[@]}"
